eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8888/eureka/

spring:
  application:
    name: account-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      label: dev
      name: application
      profile: jsf
